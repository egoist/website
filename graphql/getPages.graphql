query getPages($type: String!, $includeDrafts: Boolean, $order: Order) {
  getPages(type: $type, includeDrafts: $includeDrafts, order: $order) {
    id
    slug
    title
    contentHTML
    published
    publishedAt: dateFormatted(format: "MMM DD, YYYY", field: "publishedAt")
  }
}
