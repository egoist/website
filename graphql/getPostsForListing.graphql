query getPostsForListing {
  getPages(type: "post") {
    id
    title
    slug
    year: dateFormatted(format: "YYYY", field: "publishedAt")
    date: dateFormatted(format: "MMM DD", field: "publishedAt")
  }
}
